/**
 * @module Config
 * Config of API path
 **/

// setup api url
const Config = {
    baseURL: 'http://www.google.com/'
    // baseURL: `${apiRoot}/${siteprefix[sitename]}/api`
};

export default Config;
